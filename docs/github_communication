pkt-line format
lines:
--4B hex length (length+line+\n)
--line\n (with length specified above)
--0000_0000f
... (other lines)

GitHub communication
GET {git_url}/info/refs?service=git-receive-pack
--# service=git-receive-pack
--
--{master_sha1} {master_ref}NULL... (master_ref==refs/heads/master)

Pack file (for adding missing files to remoute server)
12B header
objects (sorted)
--variable-length header followed by compressed bdata 
20B SHA-1 hash value
